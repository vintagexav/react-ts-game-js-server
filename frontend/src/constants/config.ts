export const enum Mole {
  MOVEMENT_SPEED_MS = 777
}

export const enum Game {
  DURATION_SECONDS = 120,
  AMOUNT_OF_TILES = 12,
  RESET_TILE_ID = 0
}

export const enum ServicePlayers {
  BASE_URL = 'http://localhost:8080'
}
